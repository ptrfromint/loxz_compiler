class Nested {
    method(n) {
        fun function(n) {
            if (n <= 1) return n;

            return function(n - 1) + function(n - 2);
        }

        return function(n);
    }
}

var nested = Nested();
for (var i = 0; i <= 30; i = i + 1) {
    print nested.method(i);
}
